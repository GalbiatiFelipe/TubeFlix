version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: tubetvcontainer
    environment:
      POSTGRES_DB: tubetv
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    restart: always